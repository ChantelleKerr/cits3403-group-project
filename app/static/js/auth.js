const regoForm = document.getElementById("registrationForm");

regoForm.addEventListener('submit', (event) => {
  console.log(event);
  // http request goes here - need database and user models set up

})